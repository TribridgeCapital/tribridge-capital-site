# Remove leading zeros from numbered lists

Change the section numbering from `01, 02, 03…` to `1, 2, 3…`.

## Changes

- `src/components/focus.tsx`: `n` values become `1`, `2`, `3`.
- `src/components/approach.tsx`: `n` values become `1` through `5`.

Styling and layout stay the same.
